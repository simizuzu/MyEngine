#include "BulletManager.h"

BulletManager* BulletManager::Create(ObjObject3d* obj,const std::string filePath)
{
	return nullptr;
}

void BulletManager::Initialize(const std::string& modelName,Camera* camera)
{
	
}
